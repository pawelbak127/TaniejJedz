"""
Scraper schemas — raw platform responses + normalized output.

normalized.py  — platform-agnostic output (maps to DB models)
wolt.py        — raw Wolt API response models
pyszne.py      — raw Pyszne API response models (Sprint 3.3)
"""
